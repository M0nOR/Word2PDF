# Word2PDF
A simple app to convert several Word document files to PDF files.
Just select -> grab your Word files and drop them in the executable.
The PDFs will be saved in the same directory as the Word files.

It uses the Microsoft.Office.Interop.Word namespace, so it must be installed.
